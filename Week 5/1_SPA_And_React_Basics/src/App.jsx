import React from 'react';
import './App.css';

function App() {
  return (
    <div className="App">
      <h1>Hello React!</h1>
      <p>
        Welcome to Single Page Applications (SPA). Unlike Multi-Page Applications (MPA)
        which reload the whole page when navigating, SPAs load a single HTML page and 
        dynamically update the content as the user interacts with the app.
      </p>
    </div>
  );
}

export default App;
